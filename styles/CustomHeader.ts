import {StyleSheet} from 'react-native';
import {accentColor} from './GlobalStyles';

export const customHeader = StyleSheet.create({
  header: {
    color: accentColor,
    fontSize: 20,
    marginBottom: 10,
    fontWeight: '600',
  },
});
